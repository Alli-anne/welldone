import { loadHeaderFooter } from "./main.js";


function countWords(text) {
    try{
        const res = fetch("https://welldone-api-fm06.onrender.com/lists", {
            if () {
                
            }
        })
    }
}

document.addEventListener("DOMContentLoaded", () => {
    loadHeaderFooter();
});